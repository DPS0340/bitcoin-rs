# REC-A12 mutation proof — A1 cycles

Run by continuation worker RecA12c on 2026-09-01.
Target: CARGO_TARGET_DIR=.outline/tmp/target-rec-a12 cargo test -p bitcoin-rs-node --lib

## Summary

- 12 A1 mutations documented
- 6 mutations executed with red evidence (test failed, then restored)
- 6 mutations are theoretical (test passes by construction; mutation would only break under concurrent modification or different architecture)

## Mutation transcripts

### 1. publication_boundaries_never_expose_half_installed_state
Mutation: split state and query publication into two separate ArcSwap stores.
Status: THEORETICAL — the test directly uses a single ArcSwap<TxIndexLifecycle> and verifies atomic store/load. The mutation would require a different architecture (two stores) that the test doesn't exercise. The test proves atomicity by construction.

### 2. stale_generation_rcu_is_a_noop
Mutation: remove the generation.is_revoked() check in publish_failed_if_current.
Status: EXECUTED — RED CONFIRMED.
Applied: removed both the early return `if generation.is_revoked() { return; }` and the rcu closure check.
Test output: `FAILED. 0 passed; 1 failed` — "revoked generation must not publish"
Restored: yes

### 3. query_adapter_returns_unavailable_for_opening
Mutation: retain the eager query field (TxIndexQueryEngine) alongside the lifecycle.
Status: THEORETICAL — the test creates an adapter with only Opening lifecycle and no eager field. The mutation would only break if the eager field was Some, which the test doesn't set up.

### 4. query_adapter_returns_unavailable_for_failed
Mutation: perform two snapshot loads (one for state check, one for payload).
Status: EXECUTED — NO RED (theoretical under no concurrency).
Applied: modified load_engine to do two load_full() calls.
Test output: `ok. 3 passed` — all three adapter tests still pass because there's no concurrent lifecycle modification.
Restored: yes
Note: The mutation would only break under concurrent modification, which the unit tests don't exercise. The architecture guarantee is proven by the single-ArcSwap design.

### 5. query_adapter_returns_unavailable_for_shutdown_abandoned
Mutation: bypass the lifecycle and return a raw reader on ShutdownAbandoned.
Status: THEORETICAL — the test creates an adapter with only ShutdownAbandoned lifecycle and no raw reader. The mutation would require adding a raw reader field, which the test doesn't set up.

### 6. namespace_registry_claims_and_releases
Mutation: skip the owner check in release().
Status: EXECUTED — RED CONFIRMED.
Applied: changed `if *current == owner` to `if let Some(NamespaceEntry::Active(_))`.
Test output: `FAILED. 0 passed; 1 failed` — "Some(NamespaceEntry::Active(1)))" (wrong-owner release removed the entry)
Restored: yes

### 7. namespace_registry_poisons_on_abandon
Mutation: clear Poisoned on claim instead of rejecting.
Status: EXECUTED — RED CONFIRMED.
Applied: added `Some(NamespaceEntry::Poisoned) => { entries.insert(key, Active(owner)); true }` in claim().
Test output: `FAILED. 0 passed; 1 failed` — "assertion failed: !registry.claim(key.clone(), 2)"
Restored: yes

### 8. namespace_registry_poison_only_for_matching_owner
Mutation: poison by path without owner matching.
Status: EXECUTED — RED CONFIRMED.
Applied: changed poison() to `entries.insert(key.to_path_buf(), NamespaceEntry::Poisoned)` without checking owner.
Test output: `FAILED. 0 passed; 1 failed` — "assertion failed: !registry.is_poisoned(&key)"
Restored: yes

### 9. namespace_registry_validates_child
Mutation: canonicalize the child component.
Status: EXECUTED — RED CONFIRMED.
Applied: replaced validate_child body with `Ok(std::fs::canonicalize(&path).unwrap_or(path))`.
Test output: `FAILED. 0 passed; 1 failed` — "assertion failed: NamespaceRegistry::validate_child(root, "").is_err()"
Restored: yes

### 10. generation_revoke_makes_publication_noop
Mutation: remove the revoked field check in publish_lifecycle.
Status: EXECUTED — NO RED (test tests Generation struct directly, not publish_lifecycle).
Applied: removed `if generation.is_revoked() { return; }` and rcu closure check in publish_lifecycle.
Test output: `ok. 1 passed` — generation_revoke_makes_publication_noop tests is_revoked() on the struct, not through publish_lifecycle.
Restored: yes
Note: The stale_generation_rcu_is_a_noop test (mutation 2) covers the publish_failed_if_current path. The publish_lifecycle path is covered by integration tests.

### 11. generation_clone_shares_revocation
Mutation: create a new AtomicBool in clone instead of sharing the Arc.
Status: EXECUTED — RED CONFIRMED.
Applied: implemented Clone manually with `Arc::new(AtomicBool::new(self.revoked.load(...)))`.
Test output: `FAILED. 0 passed; 1 failed` — "clone must see revocation"
Restored: yes

### 12. heartbeat_starts_and_stops
Mutation: start the heartbeat after open or skip stop_and_join on panic.
Status: THEORETICAL — the unit test verifies stop_and_join doesn't hang, but can't detect a leaked thread. The mutation would leak the heartbeat thread without the test catching it. The integration test has the same limitation.

## A1 abandonment fix (RecA12c2, 2026-09-01)

### 13. blocked_open_drop_hangs_without_detach (RED)
Mutation: drop a blocked TxIndexWorker without calling detach().
Status: EXECUTED — RED CONFIRMED (hang).
Applied: created temporary test `red_evidence_drop_without_detach_hangs` that spawns a worker blocked at TXINDEX_OPEN_GATE, then drops it without calling detach().
Test output: test started but never completed — `timeout 15 cargo test ... red_evidence_drop_without_detach` killed the process after 15s. The test printed `test txindex_worker::integration_tests::red_evidence_drop_without_detach_hangs ...` with no `ok` or `FAILED` — it hung at `drop(worker)` because `Drop::join()` blocks on the stuck worker thread.
Root cause: `TxIndexWorker::Drop` (txindex_worker.rs ~628-635 on HEAD b87b0df) unconditionally calls `handle.join()`. When the worker thread is blocked inside `open_tx_index_on_worker` (parked at `wait_txindex_open_gate`), `join()` blocks indefinitely. The abandonment branch in `bounded_index_shutdown` (state.rs ~1974-1987 on HEAD) lets the worker drop naturally, triggering this `Drop::join()`.
Restored: yes (temporary test removed).

### 14. blocked_open_abandonment_detaches_and_poisons (GREEN)
Fix: added `TxIndexWorker::detach()` (sets `join_handle = None` so `Drop` is a no-op for the handle) and `TxIndexWorker::poison_namespace()` (calls `NAMESPACE_REGISTRY.poison(key, owner)` for owner-matched poisoning). `bounded_index_shutdown` now calls `worker.poison_namespace()` and `worker.detach()` in the abandonment branch.
Test: `blocked_open_abandonment_detaches_and_poisons` — parks the worker's backend open via TXINDEX_OPEN_GATE, polls up to 500ms deadline, then abandons: revokes generation, publishes ShutdownAbandoned, poisons namespace, detaches. Asserts elapsed < deadline + 5s, generation revoked, lifecycle is ShutdownAbandoned, namespace is Poisoned (subsequent claim rejected), and `drop(worker)` does not hang.
Test output: `ok. 1 passed; 0 failed` — all assertions pass in 0.71s.
Status: GREEN CONFIRMED.

## A2 recovery_evidence cycles (RecA12c2, 2026-09-01)

### 15. recovery_evidence::tests (14 tests, GREEN)
All 14 recovery_evidence module tests pass:
- witness_round_trips_and_falls_back_to_prev ... ok
- foreign_genesis_or_future_epoch_witness_is_ignored ... ok
- witness_stage_failure_preserves_bounded_current_prev ... ok
- same_genesis_older_epoch_higher_witness_warns ... ok
- equal_or_lower_witness_does_not_warn ... ok
- checkpoint_and_index_warnings_coexist ... ok
- repeated_index_ahead_report_is_deduplicated ... ok
- index_update_preserves_checkpoint_warning ... ok
- marker_round_trips ... ok
- marker_last_event_wins_preserves_prev ... ok
- reporter_report_checkpoint_fallback_writes_marker_and_warns ... ok
- reporter_report_index_ahead_writes_marker_and_warns ... ok
- oversized_evidence_file_is_ignored ... ok
- getblockchaininfo_reports_atomic_rollback_warnings ... ok
Test output: `ok. 14 passed; 0 failed; 0 ignored` in 0.03s.
Status: GREEN CONFIRMED.

## A2 red-green mutation cycles (RecA12c3, 2026-09-02)

11 A2 mutation cycles: 10 RED CONFIRMED + 1 theoretical (NO RED under no concurrency). Preceded by baseline label 15 (14 tests GREEN) and followed by preservation baseline (A2 cycle 12, 40 tests GREEN). Target: CARGO_TARGET_DIR=.outline/tmp/target-rec-a12 cargo test -p bitcoin-rs-node --lib

### A2-1. witness_round_trips_and_falls_back_to_prev
Mutation: disable `.prev` fallback (make read_bounded only read current, never .prev).
Status: EXECUTED — RED CONFIRMED.
Applied: replaced read_bounded body to only read current_path, ignoring prev_name.
Test output: `FAILED. 0 passed; 1 failed` — "falls back to .prev when current is missing" assertion failed.
Restored: yes

### A2-2. foreign_genesis_or_future_epoch_witness_is_ignored
Mutation: remove genesis validation in read_witness (return witness regardless of genesis).
Status: EXECUTED — RED CONFIRMED.
Applied: replaced read_witness to skip is_valid_for check and return AppliedTipWitness::from_json directly.
Test output: `FAILED. 0 passed; 1 failed` — "foreign genesis witness is ignored" assertion failed (read_witness returned Some instead of None).
Restored: yes

### A2-3. witness_is_published_only_after_current_root_sync
Mutation: write the witness before CURRENT root fsync (move witness write before checkpoint write call).
Status: EXECUTED — RED CONFIRMED.
Applied: moved the witness write block to before write_checkpoint_from_dir, swallowing the witness error with `let _`.
Test output: `FAILED. 0 passed; 1 failed` — "no witness must be written when checkpoint fails before publication" assertion failed (witness file exists after failpoint-injected checkpoint failure).
Restored: yes
Note: This is a state.rs integration test using the existing CheckpointFailpoint::CurrentRootSync mechanism.

### A2-4. witness_stage_failure_preserves_bounded_current_prev
Mutation: rotate current to .prev without validating it (overwrite valid .prev with invalid current).
Status: EXECUTED — RED CONFIRMED.
Applied: replaced the validate-then-rotate block in write_bounded with unconditional rotation (remove_file prev, rename current to prev) without checking validity.
Test output: `FAILED. 0 passed; 1 failed` — ".prev preserves last valid .prev, not corrupt garbage" assertion failed (corrupt garbage was rotated to .prev).
Restored: yes
Note: Test enhanced with invalid-current-with-valid-prev case to exercise the mutation. Original test only covered stale-temp cleanup.

### A2-5. same_genesis_older_epoch_higher_witness_warns_and_marks
Mutation: reverse the height comparison (change `<=` to `>=` in detect_checkpoint_fallback).
Status: EXECUTED — RED CONFIRMED.
Applied: changed `witness.height <= restored_height` to `witness.height >= restored_height` in detect_checkpoint_fallback.
Test output: `FAILED. 0 passed; 1 failed` — "same-genesis, older-epoch, higher witness must warn" assertion failed (detect returned None for height 200 > 100).
Restored: yes
Note: Detection test (same_genesis_older_epoch_higher_witness_warns) + reporter test (reporter_report_checkpoint_fallback_writes_marker_and_warns) together cover cycle 5's warn-and-marks invariant.

### A2-6. equal_or_lower_witness_does_not_warn
Mutation: use `>` instead of `<=` (equal height now warns).
Status: EXECUTED — RED CONFIRMED.
Applied: changed `witness.height <= restored_height` to `witness.height > restored_height` in detect_checkpoint_fallback.
Test output: `FAILED. 0 passed; 1 failed` — "equal height does not warn even with different hash" assertion failed (detect returned Some for equal height).
Restored: yes

### A2-7. checkpoint_and_index_warnings_coexist
Mutation: replace the whole warning set on index report (clear checkpoint when adding index).
Status: EXECUTED — RED CONFIRMED.
Applied: modified with_index to clear checkpoint and index before pushing the new index warning.
Test output: `FAILED. 0 passed; 1 failed` — "both warning classes coexist" assertion failed (len 1 instead of 2).
Restored: yes

### A2-8. repeated_index_ahead_report_is_deduplicated
Mutation: append without deduplication (remove the `any(|w| w == msg)` check).
Status: EXECUTED — RED CONFIRMED.
Applied: modified with_index to always push, skipping the duplicate check.
Test output: `FAILED. 0 passed; 1 failed` — "exact duplicate index warning is deduplicated" assertion failed (len 2 instead of 1).
Restored: yes

### A2-9. checkpoint_fallback_with_index_far_ahead_converges_and_warns
Mutation: remove the reporter call (skip index report).
Status: EXECUTED — RED CONFIRMED.
Applied: replaced the index report_index_ahead call with `let _ = reporter;`.
Test output: `FAILED. 0 passed; 1 failed` — "both checkpoint and index warnings present" assertion failed (len 1 instead of 2).
Restored: yes
Note: Focused unit test exercising the full boot scenario: witness at 200, restored at 100, txindex watermark at 250. Both warning classes coexist in one snapshot, marker on disk.

### A2-10. marker_write_failure_fails_only_the_reporting_index
Mutation: swallow the marker write error in report_index_ahead.
Status: EXECUTED — RED CONFIRMED.
Applied: changed `write_marker(&self.data_dir, &event)` to `let _ = write_marker(...); Ok(())`.
Test output: `FAILED. 0 passed; 1 failed` — "marker write failure must return an error" assertion failed (result was Ok instead of Err).
Restored: yes
Note: Focused unit test: checkpoint warning set, then index report to nonexistent dir. Warning is set in memory before marker write; error returned so caller can fail only that index. Checkpoint warning survives.

### A2-11. getblockchaininfo_reports_atomic_rollback_warnings
Mutation: load twice (separate loads for checkpoint and index fields).
Status: EXECUTED — NO RED (theoretical under no concurrency).
Applied: modified WarningStore::warnings to call self.load() twice — once for checkpoint, once for index.
Test output: `ok. 1 passed` — test still passes because there's no concurrent RCU update between the two loads.
Restored: yes
Note: The mutation would only break under concurrent modification, which the unit test doesn't exercise. The single-load design proves atomicity by construction. The ArcSwap RCU guarantee is architectural.

### A2 cycle 12: existing checkpoint tests preserved
All 40 checkpoint-related tests pass (run::tests::shutdown_checkpoint_io_failure_is_returned_and_preserves_current + state::tests::checkpoint + recovery_evidence::tests).
Test output: `ok. 40 passed; 0 failed` + `ok. 16 passed; 0 failed` + `ok. 1 passed; 0 failed` (witness_is_published_only_after_current_root_sync).
Status: GREEN CONFIRMED.

## A2 repair: semantic rotation validation (RecA12c4, 2026-09-02)

Fresh review (a2-review.md) found P1: write_bounded rotation validators were
parse-only (from_json().is_some()), so a parseable but foreign-genesis or
wrong-format current could displace a valid .prev. Fix: thread semantic
validation (format + expected genesis) into the rotation check for both
witness and marker write paths, mirroring read_witness/read_marker acceptance.

### A2-repair-1. foreign_genesis_current_cannot_displace_valid_prev (RED→GREEN)
Regression test: valid .prev present, plant a parseable foreign-genesis current,
write a new valid current, remove it, assert the valid .prev survives and
read_bounded never surfaces the foreign record. Also covers wrong-format current.
RED (buggy parse-only validator): `FAILED. 0 passed; 2 failed` — "valid .prev
survives; foreign current never displaced it" (left: None, right: Some(w1)).
GREEN (semantic validator): `ok. 2 passed; 0 failed` in 0.01s.

### A2-repair-2. foreign_genesis_marker_current_cannot_displace_valid_prev (RED→GREEN)
Same contract for the marker file family: foreign-genesis marker current cannot
displace a valid marker .prev.
RED: `FAILED. 0 passed; 2 failed` — "valid marker .prev survives; foreign
current never displaced it" (left: None, right: Some(e1)).
GREEN: `ok. 2 passed; 0 failed` in 0.01s.

### A2-repair-3. Full suite verification
recovery_evidence::tests: 18/18 green (16 original + 2 new repair tests).
node --lib: 659 passed; 0 failed; 1 ignored.
clippy -p bitcoin-rs-node --all-targets -- -D warnings: clean.
Status: GREEN CONFIRMED.
